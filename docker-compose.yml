version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: routes
      POSTGRES_PASSWORD: routes
      POSTGRES_DB: routesdb
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data:
