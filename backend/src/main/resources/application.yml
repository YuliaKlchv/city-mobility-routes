server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/routesdb
    username: routes
    password: routes
  jpa:
    hibernate:
      ddl-auto: update    # Automatically update the database schema
    properties:
      hibernate:
        format_sql: true
